/**
 * API base URL for the Open Food Facts proxy.
 *
 * Local (python server.py): leave empty → uses same origin (/api/...)
 * GitHub Pages + Cloudflare Worker: paste your Worker URL, no trailing slash
 *   e.g. "https://microplastic-checker.YOUR_SUBDOMAIN.workers.dev"
 */
export const API_BASE = "https://microplastic-checker.nutwara-3810.workers.dev";
